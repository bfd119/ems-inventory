rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 全コレクションに対して読み書き許可（認証なし）
    // 本番環境では認証を追加することを推奨
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
